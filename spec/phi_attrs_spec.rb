# frozen_string_literal: true

# Try to spread out test from all running in one large file,
# but if there isn't a good spot put it here for now.
RSpec.describe PhiAttrs do
  context 'global' do
    # Nothing here for now.
  end
end
